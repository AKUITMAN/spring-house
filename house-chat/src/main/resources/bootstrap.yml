server:
  port: 8082
spring:
  application:
    name: qfshop-websocket
  cloud:
    config:
      discovery:
        service-id: house-config
        enabled: true
      name: house
      profile: dev
      label: main
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka